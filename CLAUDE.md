# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

### State Management

Components use **Angular Signals** (`signal()`, `computed()`) with `OnPush` change detection. No external state library is used.
